require.config({
    baseUrl: "",
    // Initialize the application with the minesweeper file
    deps: ["js/minesweeper"],
    paths: {
        jquery: "js/lib/jquery-1.8.2.min"
    },
    shim: {}
});